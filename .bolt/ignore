# --- BOLT-FIRST, MINIMAL CONTEXT STRATEGY ---
# Start by ignoring everything, then whitelist only what Bolt needs.
**

# --- Project root files Bolt may need ---
!package.json
!package-lock.json
!pnpm-lock.yaml
!yarn.lock
!tsconfig*.json
!vite.config.*
!postcss.config.*
!tailwind.config.*
!index.html

# Keep Bolt’s own metadata available (don’t hide the engine from itself)
!.bolt/**
!.github/**

# --- Allow core src scaffolding ---
!src/
!src/main.tsx
!src/App.tsx
!src/index.css

# Shared code used across modules (keep these in to avoid “cannot find module”)
!src/components/**
!src/hooks/**
!src/lib/**
!src/types/**
!src/contexts/**
!src/utils/**
!src/mocks/**

# --- Focused module allow-list (edit this list as your “working set”) ---
# Block the whole modules tree…
src/modules/**

# …then selectively re-include only what you’re actively editing:
!src/modules/brochures/**
!src/modules/inventory-management/**
!src/modules/land-management/**
!src/modules/property-listings/**


# --- Public assets (keep lightweight, skip huge media) ---
!public/
# Allow common small assets dirs; big media stays ignored by default
public/**
!public/assets/**
!public/images/**
# (Optional) If you have big media dirs, keep them ignored:
public/videos/**
public/media/**
public/uploads/**
public/**.mp4
public/**.mov
public/**.zip

# --- Ignore common heavy / noisy stuff everywhere ---
node_modules/**
dist/**
build/**
out/**
.vite/**
.cache/**
.next/**
.nuxt/**
.parcel-cache/**
.rollup.cache/**
.svelte-kit/**
.turbo/**
.vercel/**
.yarn/**
.pnpm-store/**

# Editors, OS, logs
.vscode/**
.idea/**
*.log
*.tmp
*.swp
*.swo
*.bak

# Tests / stories / snapshots (Bolt doesn’t need these to fix app code)
**/__tests__/**
**/__mocks__/**
**/*.test.*
**/*.spec.*
**/*.stories.*
**/*.snap

# Docs & scripts not required for live fixes
docs/**
scripts/**

# Large design/source files
**/*.psd
**/*.ai
**/*.fig
**/*.sketch
